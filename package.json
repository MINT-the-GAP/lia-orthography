{
  "name": "lia-orthography",
  "version": "1.2.1",
  "description": "LiaScript plugin — orthography exercises with gated resolve, sticky solutions, and reset",
  "main": "dist/index.js",
  "scripts": {
    "build": "parcel build src/index.ts --dist-dir dist --no-scope-hoist",
    "test": "npm run test:unit && npm run test:e2e",
    "test:unit": "node --test tests/template.test.mjs",
    "test:e2e": "node --test tests/e2e/liascript-solutions.test.mjs",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm run build && npm test"
  },
  "keywords": [
    "liascript",
    "plugin",
    "orthography",
    "education"
  ],
  "devDependencies": {
    "parcel": "^2.12.0",
    "playwright": "^1.62.1",
    "typescript": "^5.3.3"
  }
}
